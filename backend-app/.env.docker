# Backend when running inside Docker. Injected by root docker-compose via env_file.
# Copy from backend-app/app/.env and set CORS_ORIGIN (and optionally GEMINI_VOICE_*) as below.
# Services resolve by container name; browser uses host ports (see root .env).

PORT=8080
CORS_ORIGIN=http://localhost:40090

# Optional: when backend needs to call Gemini Voice Agent.
# GEMINI_VOICE_HTTP_URL=http://gemini-voice-agent:3001
# GEMINI_VOICE_WS_URL=ws://gemini-voice-agent:3002
